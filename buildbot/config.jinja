{% set base = {
    'user': 'buildbot',
    'home': '/opt/buildbot',
    'install_type': 'pip',
    'install_master': 'true',
    'install_slave': 'true',
  }
%}

{%- macro base_config(key) -%}
  {%- set pillar_key = 'buildbot:base:' ~ key -%}
  {{ salt['pillar.get'](pillar_key, base.get(key)) }}
{%- endmacro -%}
